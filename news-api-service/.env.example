# Tushare 令牌，在 https://tushare.pro 注册并获取，且需开通新闻接口权限
TUSHARE_TOKEN=your_token_here
# 可选：数据库路径，默认 sqlite:///./newsdata.db
# DATABASE_URL=sqlite:///./newsdata.db
# 可选：默认抓取时间范围（小时）
# DEFAULT_FETCH_HOURS=24
